meta {
  name: 01 Login SUPER_ADMIN
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/auth/login
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "email": "cfritzsepulveda8@gmail.com",
    "password": "admin123"
  }
}

script:post-response {
  if (res.status === 200) {
    bru.setEnvVar("token", res.body.token);
  }
}

docs {
  Login plataforma Visium. Guarda el JWT en env `token`.
  Esperado: roles incluye SUPER_ADMIN; empresaIds puede tener Demo.
}
